# Render 部署配置 (Docker 模式，支持 Node + Python)
# 在 render.com 创建 Web Service 时选择 Docker

services:
  - type: web
    name: simo-api
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
